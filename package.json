{
  "name": "solitaire-library-ts-workspace",
  "version": "0.1.0",
  "description": "Workspace for TypeScript Klondike Solitaire packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace solitaire-library-ts && npm run build --workspace solitaire-library-ts-cli",
    "test": "npm run test --workspaces",
    "test:watch": "npm run test:watch --workspace solitaire-library-ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
